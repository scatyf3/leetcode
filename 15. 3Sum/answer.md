**先排序**，固定第一个数 `i`，剩下退化成**有序两数之和** —— `l = i+1, r = n-1` 相向夹逼：和小了 `l += 1`，和大了 `r -= 1`。

⚠ 这题真正的难点是**去重**，不是找解。排序后两处跳相邻重复：
1. `i > 0 and nums[i] == nums[i-1]` → `continue`（和**左**邻居比，保留第一份，所以 `[0,0,0]` 不会被丢）
2. 找到一组后 `l += 1`，再跳过所有 `nums[l] == nums[l-1]`（第一个数和左值定了，右值唯一，只跳 `l` 就够）
