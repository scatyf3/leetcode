闭区间 `[l, r]` 二分，和 704 是同一个循环，只有最后一行不同：没找到时**返回 `l`**。

⚠ 退出时 `l == r + 1`，`l` 是第一个 `>= target` 的下标，就是该插入的缝。`l` 自己能走到 `n`（全部元素都小于 target 时），所以 `r` 初值照常取 `n-1`，不用为「插到末尾」特殊处理。不能返回 `mid`：它只是最后一次迭代留下的值，在循环外没有稳定含义。等价于 `bisect_left`。
