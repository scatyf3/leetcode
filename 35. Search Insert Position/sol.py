class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        '''
        Input:
        - nums: List[int], 1 <= len <= 1e4, 升序, 元素互不相同
        - target: int, -1e4 <= target <= 1e4
        Target:
        target 在就返回下标; 不在就返回它【应该插入】的下标。必须 O(log n)
        Behavior:
        只读, 不修改输入
        Return:
        int, 下标, 取值范围是 [0, n] —— 注意 n 是合法答案(追加到末尾)
        Output: e.g. [1,3,5,6], 5 -> 2 ; [1,3,5,6], 2 -> 1 ; [1,3,5,6], 7 -> 4
        '''
        # your code here
        # need some comments to explain the logic of your code

        '''
        Dry Run:
        Test Cases:
        - [1,3,5,6], 5  -> 2   # 命中
        - [1,3,5,6], 2  -> 1   # 插在中间
        - [1,3,5,6], 7  -> 4   # 插在末尾, 返回 n ← 最容易挂的一个
        - [1,3,5,6], 0  -> 0   # 插在开头, 返回 0
        - [1], 0        -> 0
        - [1], 2        -> 1
        '''


# ============================================================
# 这题就是 295. Find Median from Data Stream 里的 binary_search
# ============================================================
#
# 你在 295 里手写过这个函数(找 num 该插进哪个位置), 这题是它的独立版。
# 先自己默写一遍再去对照 295/sol.py —— 那边的坑记在文件底部。
#
#
# ---- 写完自查 -------------------------------------------------
#
#   1. 答案的取值范围是 [0, n], 一共 n+1 个。你的搜索区间装得下 n 吗?
#      (长度 4 的数组有 5 条缝: _1_3_5_6_ )
#      -> 这决定了 r 的初值。r = n-1 会让 "插在末尾" 永远选不中。
#   2. 循环条件和区间开闭配对了吗?
#   3. 收缩时 mid 这一格本身还可能是答案吗? 可能就不能跨过它。
#   4. r == l+1 时 mid 等于谁? 区间真的缩小了吗?
#   5. 返回 l 还是 mid?
#
# 同一个思维模式在 139. Word Break 里出现过: "节点是缝不是字符"。
