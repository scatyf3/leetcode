class Solution:
    def searchInsert(self, nums: List[int], target: int) -> int:
        '''
        Input:
        - nums: List[int], 1 <= len <= 1e4, 升序, 元素互不相同
        - target: int, -1e4 <= target <= 1e4
        Target:
        target 在就返回下标; 不在就返回它【应该插入】的下标。必须 O(log n)
        Behavior:
        只读, 不修改输入
        Return:
        int, 下标, 取值范围是 [0, n] —— 注意 n 是合法答案(追加到末尾)
        Output: e.g. [1,3,5,6], 5 -> 2 ; [1,3,5,6], 2 -> 1 ; [1,3,5,6], 7 -> 4
        '''
        # your code here
        # need some comments to explain the logic of your code

        '''
        Dry Run:
        Test Cases:
        - [1,3,5,6], 5  -> 2   # 命中
        - [1,3,5,6], 2  -> 1   # 插在中间
        - [1,3,5,6], 7  -> 4   # 插在末尾, 返回 n ← 最容易挂的一个
        - [1,3,5,6], 0  -> 0   # 插在开头, 返回 0
        - [1], 0        -> 0
        - [1], 2        -> 1
        '''


# ============================================================
# 这题就是 704. Binary Search 换了最后一行
# ============================================================
#
# 用闭区间 [l, r] 模板 (paradigms/binary-search.md), 循环和 704 一字不差。
# 先自己默写一遍再去对照。
#
#
# ---- 写完自查 -------------------------------------------------
#
#   1. 答案的取值范围是 [0, n], 一共 n+1 个 (长度 4 的数组有 5 条缝: _1_3_5_6_ )。
#      r 初值是 n-1, 那答案 n 是谁走到的?
#   2. 循环条件是 l <= r 吗? 区间只剩一格 (l == r) 时还会进循环吗?
#   3. 收缩那两行都跨过 mid 了吗? (mid 已经和 target 比过了)
#   4. 退出时 l 和 r 各指着哪条缝的哪一边?
#   5. 最后一行返回 l 还是 mid?
#
# 同一个思维模式在 139. Word Break 里出现过: "节点是缝不是字符"。
