**反向多源 BFS**：两个海的整条边界各自一次性入队，**逆流而上**（只走高度 `>=` 当前的邻居），各得一个可达集合，取**交集**。

⚠ 为什么反着搜：正向是 mn 个互不相干的问题（每个格子各问一次「我能到海吗」），visited 没法复用；反向时所有格子共享同一个目标（海），可以合并成一次多源搜索。**目标端共享、起点端独立 → 从共享的那端出发。**

⚠ 为什么不能 DP：水能往四个方向流，依赖成环，排不出扫描顺序。只看左、上邻居的 DP 会漏掉「先往右下绕、再拐回上边」的格子。
