怎么判断是不是贪心？
吃所有上坡 ≥ 吃头尾说明贪心是全局最优

怎么意识到这道题吃所有上坡 ≥ 吃头尾
1. 先问比起前一题"约束放开了，多出什么自由？"
2. prices[j]-prices[i] = (p[i+1]-p[i]) + (p[i+2]-p[i+1]) + ... + (p[j]-p[j-1])
3. 既然大区间收益就是逐日差的和，那我逐日挑——正的留、负的丢——得到的和只会 ≥ 任何固定区间


你说的贪心和上一道题的退化dp的区别是？
1. 上一题还要看全局n-1 step的最优(一个scalar即可)
2. 这一题不需要维护任何状态，考虑上一时序和这一时序的数据即可

怎么训练这种直觉

做题复盘
AC 之后别急着下一题。用三个问题逼自己抽象（就像我们这几天在做的）:
1. 信号:题面里哪个词触发了这个解法?（"无限次"→ 想拆分;"O(1) 空间"→ 想滚动变量;"只依赖一侧"→ 砍数组）
2. 动作:核心那一步数学/结构动作是什么?（伸缩求和、交换论证、区间反转、双指针夹逼）
3. 边界:什么时候这招失效?（0/1 背包为什么贪心错?——因为项之间不独立）


但我有个问题，就是lc的题可以拆分成很多套路，比如我现在在刷array，但是用array也有贪心，dp之类，怎么理解这种混合性

两层收获,别只盯着一层:

载体技能(轴 A):前缀和、差分、滑动窗口、原地交换、双指针——这些是array 特有的操作手法,换成 tree 就用不上。
范式技能(轴 B):贪心/DP/二分——这些跨载体通用,你在 array 上学的贪心选择性质,到 tree、到 graph 一样用。