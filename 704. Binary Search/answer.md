闭区间 `[l, r]` 二分：`l, r = 0, n - 1`，`while l <= r`；`nums[mid] < target` 时 `l = mid + 1`，`>` 时 `r = mid - 1`（两边都跨过 mid，因为它已经和 target 比过了），相等就返回 `mid`。出循环 = 区间空了 = 没找到，返回 `-1`。

⚠ 三处要配套：`r = n - 1`、`l <= r`、`mid ± 1`。只改其中一处，就会漏掉最后一格或者死循环。
